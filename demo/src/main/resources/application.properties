spring.application.name=chat

# MySQL Database Configuration
# spring.datasource.url=jdbc:mysql://127.0.0.1:3307/chatdb

# if u use the db seperatly, use the one below
# spring.datasource.url=jdbc:mysql://docker-sql:3306/chatdb

# if u use dockercompose, use the one below
# spring.datasource.url=jdbc:mysql://mysql:3306/chatdb

spring.datasource.url=${SPRING_DATASOURCE_URL}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.sql.init.mode=always
spring.jpa.hibernate.ddl-auto=update


# for jwt
jwt.secret=travelshare-very-long-secret-key-must-be-at-least-256-bits-long-change-in-production
jwt.expiration=604800000



